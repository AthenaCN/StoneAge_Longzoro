#ifndef __NPCGEN_H__
#define __NPCGEN_H__

void NPC_generateLoop(BOOL FlgInit);

EXTERN int all_nosee;	  /* の時全ての対を no_see に  (时将全部对象设为no_see) */
EXTERN int all_nobody;	  /* の時全ての対を no_body に  (时将全部对象设为no_body) */
EXTERN int one_loop_born; /* ループで  高み出す数 oneloop_born  (循环中刷出的数量oneloop_born) */

#endif
/*__NPCGEN_H__*/
